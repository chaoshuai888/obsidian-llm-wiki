# Vault Schema

## Folder Structure
- `raw/` ¡ª input notes (immutable, never edited by olw)
- `wiki/` ¡ª AI-synthesised articles (managed by olw)
- `wiki/.drafts/` ¡ª pending human review

## Note Format
Every wiki note has YAML frontmatter with: title, tags, sources, confidence, status, created, updated.

## Links
Use `[[Article Title]]` wikilinks between notes.
